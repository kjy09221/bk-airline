<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.airline.login.LoginService">
	<select id="login_act" resultType="com.airline.login.LoginDTO">
		select * from bk_member where mem_id=#{param1} and mem_pw=#{param2}
	</select>
</mapper>